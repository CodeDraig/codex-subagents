interface:
  display_name: "Records Retention Operations"
  short_description: "Map records, retention, holds, and approval gates"
  default_prompt: "Use $records-retention-operations to review this records scope, retention authority, hold flags, disposition workflow, and approval gates."
